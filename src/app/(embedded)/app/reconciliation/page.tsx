import { ReconciliationPage } from "@/components/reconciliation/reconciliation-page";

/** Embedded reconciliation page for Shopify Admin users. */
export default function EmbeddedReconciliationPage() {
  return <ReconciliationPage />;
}

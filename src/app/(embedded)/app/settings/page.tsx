import { SettingsPage } from "@/components/settings/settings-page";

/** Embedded settings page for Shopify Admin users. */
export default function EmbeddedSettingsPage() {
  return <SettingsPage />;
}

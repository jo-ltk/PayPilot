import { TransactionsPage } from "@/components/transactions/transactions-page";

/** Standalone transactions page for finance portal users. */
export default function StandaloneTransactionsPage() {
  return <TransactionsPage />;
}

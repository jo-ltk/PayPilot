import { SettingsPage } from "@/components/settings/settings-page";

/** Standalone settings page for finance portal users. */
export default function StandaloneSettingsPage() {
  return <SettingsPage />;
}

import { ReconciliationPage } from "@/components/reconciliation/reconciliation-page";

/** Standalone reconciliation page for finance portal users. */
export default function StandaloneReconciliationPage() {
  return <ReconciliationPage />;
}
